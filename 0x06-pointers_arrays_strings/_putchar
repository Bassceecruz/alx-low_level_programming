#include <unistd.h>

/**
 * _putchar - writes the character em to stdout
 * @em: The character to print
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int _putchar(char em)
{
	return (write(1, &em, 1));
}
